<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TouchAuth" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/night003</item>
        <item name="colorPrimaryVariant">@color/night002</item>
        <item name="colorOnPrimary">@color/night001</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/night004</item>
        <item name="colorSecondaryVariant">@color/night004</item>
        <item name="colorOnSecondary">@color/night001</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>
    </style>
</resources>